[package]
name = "intiface-cli"
version = "4.0.0"
authors = ["Nonpolynomial Labs, LLC <kyle@nonpolynomial.com>"]
description = "Command Line Interface frontend for the Buttplug sex toy control library"
license = "BSD-3-Clause"
homepage = "http://intiface.com"
repository = "https://github.com/intiface/intiface-cli-rs.git"
readme = "README.md"
keywords = ["usb", "serial", "hardware", "bluetooth", "teledildonics"]
edition = "2018"

[dependencies]
# buttplug = { path = "../buttplug-rs/buttplug" }
buttplug = "0.3.1"
argh = "0.1.3"
async-std = { version = "1.5.0", features = ["unstable", "attributes"] }
env_logger = "0.7.1"
# This needs to match the version of openssl in ws in buttplug, so we can make
# sure we're compiling the correct thing. Using VSPKGRS_DYNAMIC here would
# require us to also redistribute the openssl DLL.
openssl = { version = "0.10.29", features = ["vendored"] }
rcgen = "0.8.1"
async-tungstenite = "0.4.2"
log = "0.4.8"
futures = "0.3.5"
async-tls = "0.7.0"
rustls = "0.17.0"
prost = "0.6.1"
lazy_static = "1.4.0"
bytes = "0.5.4"

[target.'cfg(windows)'.dependencies]
kernel32-sys = "0.2.2"
winapi = "0.3.8"

[build-dependencies]
prost-build = "0.6.1"
vergen = "3.1.0"
